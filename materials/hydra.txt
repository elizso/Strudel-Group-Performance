// part 1: listening to music, and chilling 

// in part 1.5 and 2 you can change the shape to whatever you want :) 
voronoi(10, 0.5, 0.3).modulate(noise(3), 0.1)
  .blend(shape([12, 11, 12, 2, 4, 5, 5, 4]))
  .diff(gradient(1).pixelate(40,20), 0.01)
  .out()


// // part 1.5: inspired to play
s0.initScreen()
src(s0).blend(gradient(2)).out()

// "trombone" solo for 20 seconds, gamepad joins in with it's space sounds 

// part 2: create mUsIc wItH a DicE (for whoever is playing with a dice)
s0.initCam()
src(s0).pixelate(200,100).contrast(() => Math.sin(time) * 5).out()

